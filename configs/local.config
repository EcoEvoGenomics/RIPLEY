// Site configuration: run all tasks on the local machine.
//
//   ./RIPLEY <module> <options.yaml> config/local.config
//
// Requires local installation of Podman to handle containers.

process.executor = "local"
podman.enabled = true
